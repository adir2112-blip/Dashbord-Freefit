// Template — copy this file to config.js and fill in real values.
// config.js itself is gitignored, so real credentials never reach the repo.
const FB_TOKEN = 'YOUR_FIREBERRY_TOKEN_HERE';
const CX_USER = 'YOUR_3CX_USERNAME_HERE';
const CX_PASS = 'YOUR_3CX_PASSWORD_HERE';
